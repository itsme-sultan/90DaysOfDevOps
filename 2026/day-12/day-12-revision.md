# Breather & Revision (Days 01–11)


- Which 3 commands save you the most time right now, and why?
  1. ` ls -l ` : most useed command, helps to check file owner and group and its permission.
  2. `systemctl status ` : if service failed, this command teel everything about service status.
  3. `chmod ` : helps to give right permission to the file.
     
- How do you check if a service is healthy? List the exact 2–3 commands you’d run first.
  1. `systemctl status` : check the service status
  2. `journalctl -u ` : check the service
  3. `systemctl enable ` : enable the service if failed to start at reboot
     
- How do you safely change ownership and permissions without breaking access? Give one example command.
  1. chown : use to chnage the file ownership.
  2. chmod : use to chnage the file permission.
     
- What will you focus on improving in the next 3 days?
  1. focusing on practicing the commad in real environment.
  2. revising the core concept.
